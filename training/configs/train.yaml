run_dir: training/runs/perkunas_3050_pilot_test
model_config: training/configs/model_3050_pilot.yaml
train_shards_glob: training/data/perkunas_pilot/tokenized/train_*.npy
val_shards_glob: training/data/perkunas_pilot/tokenized/val_*.npy
tokenizer_dir: training/tokenizer/perkunas-pilot-tokenizer
seed: 1337
batch_size: 1
gradient_accumulation_steps: 4
max_steps: 30000
eval_interval: 150
save_interval: 1500
log_interval: 5
learning_rate: 0.0005
weight_decay: 0.1
beta1: 0.9
beta2: 0.95
warmup_steps: 200
min_lr_ratio: 0.1
mixed_precision: fp16
resume_from: null
require_gpu: true
num_workers: 0
deterministic: false